class CropModel {
  String? crop_title;
  int? crop_release_year;
  String? crop_poster_url;
  double? rating;

  CropModel(this.crop_title, this.crop_release_year, this.rating,
      this.crop_poster_url);
}
